import React from 'react';

const UploadBox = () => {
  // This component is not used in the current implementation
  // It's here for future expansion if needed
  return null;
};

export default UploadBox;